# App Track — source stays in its own git; extras only stores the Arch package.
NAME=app-track
SRC=../app-track
GIT=https://github.com/RishiSpace/app-track.git
PKGBUILD_DIR=packaging/arch
PKGBUILD=PKGBUILD.local
