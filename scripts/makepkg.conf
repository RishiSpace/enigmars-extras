# extras publishes install packages, not debuginfo.
# archlinux:latest enables `debug` in OPTIONS; that requires debugedit
# and would emit *-debug-* tarballs we do not want in the pacman db.
source /etc/makepkg.conf
OPTIONS=(strip docs !libtool !staticlibs emptydirs zipman purge !debug !lto)
